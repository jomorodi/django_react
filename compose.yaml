services:
  api:
    build: ./Config/Backend
    ports:
      - "9006:9002"
    volumes:
      - ./backend:/backend
    command: bash -c "python manage.py runserver 0.0.0.0:9002"

  web:
    build: ./Config/Frontend
    ports:
      - "3006:5173"
    volumes:
      - ./frontend:/frontend
